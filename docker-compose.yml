version: '3.8'
services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - type: bind
        source: ./user_preferences.json
        target: /app/user_preferences.json
        read_only: true
    restart: unless-stopped