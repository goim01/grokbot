version: '3.8'
services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./user_preferences.json:/app/user_preferences.json
    restart: unless-stopped